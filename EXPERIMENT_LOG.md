# VITA-1.5 强化学习实验全过程记录

> **文档定位**：这是一份**完整过程记录**，不是结果摘要。
> 六轮实验按时间顺序展开，包含每一步的动机、实测数字、
> 走过的弯路、以及我判断错又更正的地方。
>
> 采集于 2026-08-09 / 08-10，8×H100 单机。
> 所有数字由 `tools/summarize_all_runs.py` 等脚本从结果文件直接提取，
> 未经手工转录。复现步骤见 [§10](#10-复现步骤)。

---

## 目录

| 章节 | 内容 | 什么时候看 |
|---|---|---|
| [§1 摘要](#1-摘要一页看完) | 六轮结果一页表 | **只想看结论** |
| [§2 实验设计](#2-实验设计) | 为什么这样设计 | 想理解方法论 |
| [§3 起点：修通评测](#3-第零步把评测链路修通) | 四个阻断性问题 | 复现环境时 |
| [§4 数据](#4-数据rlaif-v) | RLAIF-V 与转换要点 | 换数据集时 |
| [§5 第 1–3 轮：DPO 无效](#5-第-13-轮dpo-在原始基座上无效) | 三轮排除法 | 想看诊断过程 |
| [§6 根因定位](#6-根因两层诊断) | 探针 + 元数据发现 | **核心方法论** |
| [§7 第 4 轮：on-policy 自采样](#7-第-4-轮on-policy-自采样) | 修候选，判据仍是瓶颈 | |
| [§8 第 5 轮：全参 SFT](#8-第-5-轮全参-sft) | 绕开判据 | |
| [§9 第 6 轮：SFT+DPO](#9-第-6-轮sft-起点上的-dpo) | **最终有效方案** | |
| [§10 复现步骤](#10-复现步骤) | 可直接跑的命令 | **想复现** |
| [§11 踩过的坑](#11-踩过的坑) | 12 个环境/工程问题 | 避免重复 |
| [§12 我判断错的地方](#12-我判断错又更正的地方) | 5 处更正 | 想知道哪些结论改过 |
| [§13 产出清单](#13-产出清单) | 工具与文件位置 | 找东西 |

---

## 1. 摘要（一页看完）

### 1.1 六轮实验做了什么

| 轮 | 干预 | 假设 | 结果 |
|---|---|---|---|
| **0** | 修通 VLMEvalKit | — | 四项 baseline 对齐论文（此前仓库零个数字） |
| **1** | DPO 3000 对 / batch 16 / 5e-6 | 直接训就有效 | ❌ 六项全在噪声内 |
| **2** | DPO 同上，LR × 4 | 学习率不够 | ❌ 训练更好，评测持平 → **排除调参** |
| **3** | DPO 15000 对 / batch 63 | 规模不够 | ❌ 模型真动了（漂移 6.7×），但净效应零 → **排除规模** |
| **4** | on-policy 自采样偏好对 | 数据分布外 | ⚠️ 方差降 26%，但判据噪声让 accuracy 上不去 |
| **5** | 全参 SFT（只用 chosen） | 基座建模不足 | ⚠️ 可分性 53.6% → **58.0%**，但通用能力小降 |
| **6** | **SFT 起点 + DPO** | 组合上述两点 | ✅ **POPE 幻觉率 −2.15pt（p=4.2×10⁻⁵）** |

### 1.2 最终数字（六模型 × 八指标）

| 指标 | baseline | A | B | C | SFT | **SFT+DPO** |
|---|---|---|---|---|---|---|
| MME total | 2353.51 | 2352.38 | 2352.11 | 2346.25 | 2273.17 | 2281.66 |
| **MME 逐题%** | **88.50** | 88.50 | 88.50 | 88.54 | 88.12 | 88.37 |
| MMStar | 59.80 | 59.80 | 59.93 | 60.07 | 58.67 | 58.67 |
| **MMBench** | 77.79 | 77.71 | 77.79 | 78.02 | 78.48 | **79.26** |
| AI2D | 79.24 | 79.27 | 79.27 | 79.44 | 79.11 | 79.31 |
| **POPE 幻觉%** | 10.97 | 11.00 | 11.11 | 10.97 | 10.34 | **8.82** |
| Hallu aAcc | 61.93 | 61.93 | 61.83 | 61.83 | 60.57 | 60.67 |
| Hallu fAcc | 37.57 | 37.57 | 37.28 | 37.28 | 33.82 | 33.53 |

**相对 baseline 的变化**（正数为改善，POPE 幻觉率为负数才是改善）：

| 指标 | A | B | C | SFT | **SFT+DPO** | 噪声带 |
|---|---|---|---|---|---|---|
| MME 逐题 | ±0.00 | ±0.00 | +0.04 | −0.38 | −0.13 | — |
| MMStar | ±0.00 | +0.13 | +0.27 | −1.13 | −1.13 | ±2.48 |
| **MMBench** | −0.08 | ±0.00 | +0.23 | +0.70 | **+1.47** | ±2.27 |
| AI2D | +0.03 | +0.03 | +0.19 | −0.13 | +0.06 | ±1.43 |
| **POPE 幻觉率** | +0.03 | +0.14 | ±0.00 | −0.63 | **−2.15** | — |
| Hallu aAcc | ±0.00 | −0.11 | −0.11 | −1.37 | −1.26 | — |
| Hallu fAcc | ±0.00 | −0.29 | −0.29 | −3.76 | −4.05 | — |

> **这张表最值得读的是形状，不是单个数字。**
>
> 前三轮（A/B/C）**所有格子都在 ±0.3 以内**——这是无效干预的标准形状。
> **SFT+DPO 是唯一一列有大幅正负分化的**：POPE −2.15（显著）、
> MMBench +1.47，代价是 fAcc −4.05。
> **有代价的真实改进，与噪声可区分。**

### 1.3 一句话结论

**在幻觉专项上显著变好，通用 MCQ 基本持平，视觉错觉类退化。**

RLAIF-V 是为降低幻觉设计的数据集，POPE 是最对口的尺子，
而它恰好是提升最大的那一项（−2.15pt，假阳性少 78 例，p<0.0001）。
方向完全正确，但不是全面提升。

---

## 2. 实验设计

**目标**：给 VITA-1.5（7B 多模态模型）加一个 RL 阶段，并测出它有没有用。

### 2.1 为什么需要先设计

此前本仓库**一个 benchmark 数字都没有**——`import vlmeval` 就失败。
没有 baseline，训练后哪怕拿到一个数，也无法判断是提升、退化还是噪声。

所以整个实验分两步：

1. **先建立可信的标尺**：跑通评测，且数字必须落在论文附近。
   一个数字仅仅「存在」证明不了什么；**落在论文值附近才同时证明了
   prompt 模板、图像预处理、答案抽取三件事都对**。
2. **再做干预并对比**：同一套标尺量 before/after。

### 2.2 三条贯穿全程的方法论原则

**① 每个 delta 必须和噪声带一起报。**
MMStar 上 1.96σ 是 ±2.5 分——不知道这个数的话，`+0.3` 会被当成提升。
噪声带按二项分布算：`1.96 × sqrt(p(1-p)/n)`，n 取**独立题目数**
（MMBench 是 1292 而非 4876 行，因为 circular eval 把一题展开成多行）。

**② 每个候选原因用一个专门的对照实验排除，而不是一起调。**
这是第 1–3 轮的核心：LR × 4 单独一轮、规模 × 5 单独一轮，
这样每一轮的结果都能独立解读。

**③ 训练前先量数据，不要事后解释。**
这条是第 3 轮之后才学到的（见 §6），代价是约 4 小时 GPU 时间。

### 2.3 评测集选择与污染控制

选了六个**规则判分、不需要 GPT judge** 的 benchmark：

| Benchmark | 题数 | 类型 | 为什么选 |
|---|---|---|---|
| MME | 2374 | Yes/No | 论文报了，可对齐 |
| MMStar | 1500 | 选择题 | 去掉了能靠文本先验蒙对的题 |
| MMBench_DEV_EN_V11 | 4876 行 / 1292 题 | 选择题 + circular | 论文报了 |
| AI2D_TEST | 3088 | 选择题 | 论文报了 |
| **POPE** | 5127 | Yes/No | **幻觉专项**，71% 问不存在的物体 |
| **HallusionBench** | 951 | Yes/No | **幻觉专项**，视觉错觉图 |

**污染控制**：训练数据（RLAIF-V）的图和问题来自 VQA **train** split
（VQAv2 / OK-VQA / GQA / TextVQA / COCO），**与上述六个评测集无重叠**。
这一点是所有 before/after 数字的前提。

---

## 3. 第零步：把评测链路修通

`import vlmeval` 失败，背后叠了**四个独立问题**。按踩到的顺序：

### 3.1 antlr4 / omegaconf 版本冲突

```
Exception: Could not deserialize ATN with version 3 (expected 4).
```

`omegaconf 2.3.1` 的语法文件由 antlr4 4.9 系列生成，企业镜像最低 4.11。

**解**：从上游 PyPI 装 4.9.3。**它是纯 Python 包**——这点关键，
因为本机 gcc 4.8.5 编译不了任何东西。

```bash
pip install --index-url https://pypi.org/simple/ "antlr4-python3-runtime==4.9.3"
```

降级后复查 `torch 2.3.1+cu121`、`transformers 4.41.1` 均未受影响。

### 3.2 数据集根目录被硬编码成上游作者的机器

`vlmeval/smp/file.py:71` 写死 `/mnt/cfs/lhj`。本机没有，
于是它**无视已下载的文件、每次都重新下载**。设 `LMUData` 环境变量即可。

**这个坑最坑的地方是它伪装成别的问题**：你看到的是 SSL 报错（见 3.3），
真正的原因隔了两层。

### 3.3 opencompass 服务器证书过期

```
NSS error -8181 (SEC_ERROR_EXPIRED_CERTIFICATE)
```

`openssl s_client` 查得 `*.openxlab.space` 叶证书 `NotAfter: Apr 16 2026`，
而当天是 8 月 9 日——**服务端过期，不是本机 CA 库问题**
（换 certifi 的 bundle 一样失败）。

**解**：`curl -k` 下载后**用 VLMEvalKit 源码里存的 MD5 校验**。
MD5 来自 git 仓库，是独立于传输通道的可信来源，篡改会被检出——
**这比信任一张过期证书更严格**。六个数据集全部校验通过。

### 3.4 pyarrow 必须走预编译 wheel

`pip install pyarrow` 尝试源码编译并死在 gcc 4.8.5。
`pip install --only-binary=:all: "pyarrow==16.1.0"` 秒过。

### 3.5 修通后的 baseline

| Benchmark | 实测 | 论文 | 差 |
|---|---|---|---|
| MME | **2353.51** | 2362.0 | −0.36% |
| MMStar | **59.80** | 60.2 | −0.4 |
| MMBench | **77.79** | 76.6 | +1.2 |
| AI2D | **79.24** | 79.3 | −0.06 |

**四项全部落在论文 ±1.2 内。** 这是整个实验的地基：
它同时证明了 prompt 模板、图像预处理、答案抽取三件事都对。

单卡 H100 跑完四项约 40 分钟（MME 2374 题 7 分 10 秒，峰值显存 18.4 GB），
这让 before/after 对比成为分钟级操作。

---

## 4. 数据：RLAIF-V

**RLAIF-V-Dataset**（openbmb），14 个分片约 83000 对，实际用了 4 个分片。
转换脚本 `tools/make_rlaif_v_data.py`。

### 4.1 转换脚本必须处理的三件事

| 事项 | 不做会怎样 |
|---|---|
| **图像按内容寻址**（SHA-1 做文件名） | parquet 里图是内嵌字节且跨行重复。15000 对实际只有 **8821 张**不同的图 |
| **补 `<image>` token** | `LazySupervisedDataset` 靠这个字面量决定往哪插视觉特征，**缺了会静默退化成纯文本训练**，loss 曲线照常好看 |
| **丢掉两侧相同的 pair** | DPO logit 恒为 0，loss 永远 `-log(0.5)`，白跑前向。实测 15297 条里有 21 条 |

### 4.2 数据体检（训练前就该看的数字）

| 指标 | 3000 对 | 15000 对 | 判读 |
|---|---|---|---|
| chosen 平均长度 | 299 字符 | 307 | |
| rejected 平均长度 | 298 字符 | 307 | 几乎相同 |
| chosen 更长的比例 | 46.5% | 47.1% | **无长度偏置**（50% 为完全无偏） |
| 两回答文本相似度 | 0.29 | 0.294 | **是两个不同的答案**，不是改词 |

**这两个「好」指标是双刃剑**：没有长度捷径、没有格式捷径，
两个回答都流畅切题、只在某个细节是否幻觉上不同。
**RLAIF-V 作为高质量偏好数据的优点，正是它对 7B 基座很陡的原因。**

### 4.3 链路正确性检查：首步 loss = 0.6931

**这是数学恒等式，不是经验值**：未训练的 LoRA 让 policy 等于 reference，
DPO logit 为零，loss 必然是 `-log(0.5) = 0.6931`。

**这是判断参考模型接线是否正确的最强信号，而且免费。**
合成数据和真实数据上都精确命中。

> 附带发现：冒烟测试里 `lora_dropout` 必须设 0。
> dropout 在 `model.train()` 下对 policy 生效，而参考模型关了 adapter
> 因此没有 dropout——两侧不对称，首步就不等于 0.6931，这条检查会失效。

---

## 5. 第 1–3 轮：DPO 在原始基座上无效

### 5.1 三轮配置与训练指标

| 轮 | 数据 | 有效 batch | LR | 步数 | 耗时 | accuracy | margin | 策略漂移 | loss(末5) | grad_norm |
|---|---|---|---|---|---|---|---|---|---|---|
| **A** | 3000 | 16 | 5e-6 | 187 | 50 min | 0.520 | +0.0026 | 0.27% | 0.6916 | 0.39 |
| **B** | 3000 | 16 | **2e-5** | 187 | 47 min | 0.539 | +0.0047 | 0.37% | 0.6893 | 0.42 |
| **C** | **15000** | **63** | 2e-5 | 238 | 143 min | **0.604** | **+0.0473** | **1.81%** | **0.6724** | 0.30 |

> **「策略漂移」= `|policy_logp − ref_logp| / |seq_logp|`**，
> 即模型相对基座偏离了多少。
> **benchmark 响应的是这个量，不是 margin。** 这个区分很重要。

### 5.2 C 轮：唯一"训起来"的一轮

把有效 batch 从 16 提到 63（7 卡 × 累积 9）之后：

- **loss 首次真正跌破 0.69**：A/B 组末段 0.6916/0.6893 距离 0.6931
  只有 0.002–0.004，基本还贴在零点；C 组到 0.6724。
- **margin 是 A 组的 18 倍**，accuracy 0.604。
- **策略漂移是 A 组的 6.7 倍**。
- **grad_norm 从 0.39 降到 0.30**——平均 63 个样本后梯度噪声确实降了，
  符合 √G 的预期。

按五等分看 C 组的漂移演化（n=2142）：

| 阶段 | 策略漂移 | margin | accuracy |
|---|---|---|---|
| Q1 | 0.266% | +0.0039 | 0.521 |
| Q2 | 0.458% | +0.0084 | 0.563 |
| Q3 | 1.283% | +0.0386 | 0.607 |
| Q4 | 1.788% | +0.0501 | 0.624 |
| Q5 | **1.959%** | +0.0467 | 0.579 |

**漂移全程涨 7.4 倍**，而 A/B 组是全程卡在 0.27% 不动。
注意 Q3→Q5 漂移仍在涨但 margin/accuracy 已见顶回落——
**这正是「继续沿噪声方向前进」的样子：位移还在累积，收益已经没有了。**

### 5.3 但评测全部在噪声内

见 §1.2 那张表：A/B/C 三列所有格子都在 ±0.3 以内。

**POPE 上的 McNemar 检验最能说明问题**：

| 轮 | 改变题数 | 改对 | 改错 | p |
|---|---|---|---|---|
| A | 5 / 5127 | 2 | 3 | 1.000 |
| B | 13 / 5127 | 4 | 9 | 0.267 |
| **C** | **24 / 5127** | **12** | **12** | **1.000** |

**C 组那一行是最干净的证据**：改变的题数是 A 组的 5 倍，
但 12 改对 / 12 改错、Yes→No 与 No→Yes 各 12 题——**完全对称**，
所有聚合指标逐位相同。

**这是「噪声驱动更新」的标准 signature：位移变大，净方向为零。**

---

## 6. 根因：两层诊断

### 6.1 第一层：可分性探针

写了 `tools/probe_preference_separability.py`：**用冻结的基座直接给
chosen/rejected 打分，看它能不能分对**。

```
3000 对那批（n=400）:  55.2%  CI [50.4%, 60.1%]  p=0.036  信噪比 0.11
15000 对那批（n=500）: 52.2%  CI [47.8%, 56.6%]  p=0.325  信噪比 0.055
两批合并（n=900）:     53.6%  CI [50.3%, 56.8%]  p=0.033
```

**判别力只比随机高 3.6 个百分点。** log-prob 差均值 +3.89，
标准差 35.87——**信噪比 0.11**。

DPO 的梯度正比于「模型当前错得有多系统」。信噪比 0.11 意味着
**每个 batch 的梯度方向几乎被噪声主导**。

### 6.2 第二层：为什么不可分（元数据发现）

查 RLAIF-V 的 `origin_split` 字段：

| 字段 | 值 |
|---|---|
| 生成 chosen/rejected 的模型 | **OmniLMM-12B** |
| 判定优劣的模型 | **OmniLMM-12B** |

**两个回答都不是 VITA 写的，优劣也不是按 VITA 的错误模式标的。**

DPO 的前提是「模型自己会犯这个错，我们纠正它」。
RLAIF-V 把任务变成了「**评价另一个 12B 模型的两个回答哪个好**」
——这是分布外任务。

**这一层解释了前三轮的全部现象**：
加学习率没用（方向本就与 VITA 无关）、
加 batch 让漂移涨 6.7 倍却不变好（放大的是一个无关方向）。

### 6.3 逐个排除的完整清单

| # | 候选原因 | 排除方式 | 结果 |
|---|---|---|---|
| 1 | 梯度消失 | grad_norm | 全程 0.20–0.45，健康 → 排除 |
| 2 | 参考模型接错 | 首步 loss | 精确 = 0.6931 → 排除 |
| 3 | 数据格式错 | 校验 `<image>` token / 图像 / pair 结构 | 全部正确 → 排除 |
| 4 | **学习率不够** | **B 轮：LR × 4** | 训练指标更好，评测完全一样 → 排除 |
| 5 | **benchmark 不对口** | **POPE + HallusionBench** | 5127 题只动 5 题、p=1.000 → 排除 |
| 6 | **规模不够** | **C 轮：数据 ×5、batch ×4** | 模型真动了但净效应零 → 排除 |
| 7 | **数据可分性不足** | **探针 + 元数据** | **53.6%、且数据为他模型自产自判 ← 根因** |

### 6.4 探针的判读标准（本项目自己立的）

| 探针结果 | 判读 | 建议 |
|---|---|---|
| ~50%，CI 跨过 50% | 不可分 | 换数据，别训 |
| 显著但信噪比 < 0.2 | **真实但太弱** | 训得起来，但不会变好 |
| 65%+ | 信号充足 | 可以训；此时 margin 还平坦才该查 trainer |

**探针至少跑 300–400 对**：n=100 的置信区间宽到无法区分
「随机」和「弱信号」（这是我踩过的坑，见 §12.1）。

---

## 7. 第 4 轮：on-policy 自采样

### 7.1 思路

既然问题是「回答不是 VITA 写的」，那就让 VITA 自己写：
对同一张图+问题采样 K=6 个回答，**两侧都来自 VITA**，
天然落在它自己的决策边界上。

工具：`tools/make_selfsample_data.py`，8 卡并行 15 分钟产出 2400 对。

### 7.2 判优劣：试过三种，两种不成立

| 判据 | 结果 | 为什么 |
|---|---|---|
| **多数票（自一致性）** | ❌ 不成立 | 实测 2/5 票选了错误答案；裸的 `1800000` 赢过正确完整句；自由问答的复述根本不聚类（有的每个样本自成一组） |
| **长度/格式启发式** | ❌ 直接否掉 | 等于给 DPO 一个与质量无关的捷径 |
| **与 RLAIF-V chosen 的相似度** | ⚠️ 采用 | 参考答案只决定「VITA 的哪个回答更好」，本身不进训练数据 |

### 7.3 结果：方差降了，accuracy 没上去

| 指标 | RLAIF-V（off-policy） | 自采样（on-policy） | 变化 |
|---|---|---|---|
| accuracy | 53.6% | 54.8% | +1.2pt（不显著） |
| logp gap 均值 | +3.89 | +3.77 | — |
| **logp gap 标准差** | 35.87 | **26.72** | **−26%** |
| **信噪比** | 0.108 | **0.141** | **+30%** |

> **这两个数说的是不同的事，值得分清**：
> **accuracy 衡量「方向对不对」**，由判优劣的判据决定；
> **sd 衡量「打分稳不稳」**，由候选回答来自谁决定。
>
> **on-policy 确实起了它该起的作用（方差降 26%），
> 但判据的噪声决定了 accuracy 上不去。**

### 7.4 判据的天花板（实测）

词袋相似度**约 1/10 的对判反**。典型案例：

```
问：这些工具更可能是皮革匠还是纸艺匠用的？
参考答案（RLAIF-V chosen）：皮革匠……（含 tools/hole/punch/scissors 等词）

VITA 的两个采样：
  「纸艺匠…」  与参考共享 hole/punch/scissors/crafting 四个无关词  → 0.50
  「皮革匠…」  只有 leather 一个决定性词                        → 0.31
→ 判反了
```

**给「分歧词」加权无效**，因为那些无关词也在分歧集里。

**追加验证：筛「高置信」对也没用。**

| 数据 | accuracy | 信噪比 |
|---|---|---|
| 自采样 全量 | 54.8% | 0.141 |
| 自采样 **高置信子集**（相似度差 >0.35，634 对） | **53.2%** | 0.117 |

**高置信子集反而更低。** 这否掉了「筛数据」这条廉价路线，
同时说明判据坏在哪：

> **参考相似度差距衡量的是「词面重叠差多少」，
> 不是「判对的概率有多高」。** 差距大往往只意味着
> 一个回答啰嗦、另一个简短，与哪个正确无关。

### 7.5 这一轮的结论

**换成 on-policy 解决了方差，但没解决判据。**
信噪比 0.141 仍低于 0.2 门槛，**按本项目自己的标准不该直接训**。

剩下两条路：**语义判据**（NLI / LLM 评分）或**先做 SFT**。
选了后者，因为它**完全不需要判优劣**。

---

## 8. 第 5 轮：全参 SFT

### 8.1 为什么 SFT 能绕开难题

**SFT 只需要好答案，不需要知道哪个更好。** 所以同一批数据，
扔掉 `rejected`、只用 `chosen` 做监督训练，判据问题自动消失。

而且它**直接攻击测出来的根因**：探针说基座分不开这些 pair，
这句话的另一面是**基座对这个分布建模得不好**。
SFT 提升分布内能力；如果有效，**之后 DPO 的可分性会自然改善**
——因为可分性是模型的属性，不只是数据的属性。

### 8.2 配置与关键决策

数据：`tools/make_rlaif_v_sft_data.py`，20000 样本、11042 张唯一图、
目标长度均值 323 字符。

| 决策 | 值 | 理由 |
|---|---|---|
| **ZeRO-3**（不是 zero2） | — | 全参 7B 单卡需 ~98 GB（bf16 权重 14 + fp32 主权重 28 + AdamW 双动量 56）。zero2 只分片优化器状态、参数仍复制。之前 DPO 能用 zero2 是因为 LoRA 可训练参数极少 |
| **lr 1e-6** | 比 LoRA DPO 低一个数量级 | 基座已校准良好（MME 2353 vs 论文 2362），风险是**用 2 万条 VQA 答案覆盖掉通用能力**，不是欠拟合。与上游 finetune 脚本一致 |
| **冻结视觉塔 + 音频编码器** | — | 2 万条答案远不足以重训感知，解冻是最快搞坏 benchmark 的方式 |
| **workers=0** | — | 512MB 的 `/dev/shm` 装不下 8 卡 × 4 worker（见 §11.7） |

8 卡 ZeRO-3、625 步、**1 小时 54 分**、约 30 GB/卡。

### 8.3 训练：本项目第一条正常的学习曲线

| 阶段 | loss |
|---|---|
| Q1 | 1.1365 |
| Q2 | 1.0124 |
| Q3 | 0.9960 |
| Q4 | 0.9718 |
| Q5 | 0.9696 |

首 5 步 → 末 5 步：**1.2307 → 0.9751，降 21%**，末段走平。

**对比 DPO：**

| | loss 变化 | 幅度 |
|---|---|---|
| DPO A/B（batch 16） | 0.6931 → 0.6913 | 0.3% |
| DPO C（batch 63） | 0.6931 → 0.6724 | 3% |
| **SFT** | **1.2307 → 0.9751** | **21%** |

差 7 倍不奇怪：**SFT 学「复现这个答案」（模型有能力学），
DPO 学「把 A 排在 B 前面」（基座判别力 53%，目标本身模糊）。**

### 8.4 关键结果：可分性大幅提升

用**同一批 15000 对**数据，SFT 前后分别由基座打分：

| 指标 | SFT 前 | SFT 后 | 变化 |
|---|---|---|---|
| accuracy | 52.2% | **58.0%** | +5.8pt |
| vs 随机 | p=0.325（不可区分） | **p=0.001（显著）** | 质变 |
| logp gap 均值 | +1.84 | **+6.04** | 3.3× |
| logp gap 标准差 | 33.51 | 27.76 | −17% |
| **信噪比** | 0.055 | **0.218** | **4.0×** |

**信噪比首次跨过 0.2 门槛。** 机制得到验证。

> **统计口径要说准**：「SFT 后模型能显著区分这些 pair」这条很硬
> （p=0.001，CI [53.2%, 62.8%] 完全不跨 50%）。
> 但「提升本身显著」只是边缘——两比例检验 z=1.74、**p=0.082，未过 0.05**。
> 更可信的是信噪比 4 倍：它由均值涨 3.3 倍和标准差降 17% 共同驱动，
> 两个方向都对，不是抽样噪声能造出的形状。

### 8.5 代价：通用能力小幅下降

| Benchmark | baseline | SFT | Δ |
|---|---|---|---|
| MME 逐题% | 88.50 | 88.12 | **−0.38** |
| MMStar | 59.80 | 58.67 | −1.13 |
| **MMBench** | 77.79 | **78.48** | **+0.70** |
| AI2D | 79.24 | 79.11 | −0.13 |
| **POPE 幻觉率** | 10.97% | **10.34%** | **−0.63（p=0.027 显著）** |
| Hallu aAcc | 61.93 | 60.57 | −1.37 |
| Hallu fAcc | 37.57 | 33.82 | −3.76 |

**损伤不均匀，而且方向可解释**：

| 受益（与 RLAIF-V 分布对齐） | 受损（不对齐） |
|---|---|
| POPE 物体存在（p=0.027） | `OCR`：改变 5 题、**5 题全错** |
| MMBench 逻辑推理 +4.84 | `code_reasoning` −2 |
| MME `artwork` +7、`posters` +3 | HallusionBench 视觉错觉 |

**这是标准的分布迁移形状**，不是「SFT 有效」或「SFT 无效」能概括的。

### 8.6 两个幻觉 benchmark 为什么打架

| Benchmark | 真值 Yes 占比 | baseline 答 Yes | SFT 答 Yes |
|---|---|---|---|
| POPE | 29.3% | 34.0% | **33.4%（↓，有帮助）** |
| HallusionBench | 42.2% | 66.6% | **71.1%（↑，有害）** |

**SFT 并没有让模型「一律更谨慎」，而是让它在不同任务上朝不同方向偏。**

RLAIF-V 训的是「描述图片时别编造物体」：
迁移到了 POPE 的物体存在判断，**但没有迁移到 HallusionBench 的
「抵抗视觉错觉」**——后者需要对反直觉证据的推理，不是描述的克制。

---

## 9. 第 6 轮：SFT 起点上的 DPO

### 9.1 配置

**与 C 轮完全相同**（15000 对、有效 batch 60、lr 2e-5），
**唯一的变量是起点**：从 SFT checkpoint 而非原始基座出发。

6 卡 × 累积 10、250 步、**2 小时 26 分**。

首步 `rewards/chosen = rewards/rejected = 0.0` 精确为零——
确认参考模型正确指向了 SFT 权重。

### 9.2 训练：四轮里最强

| 轮 | 起点 | accuracy | margin | 策略漂移 | loss(末5) |
|---|---|---|---|---|---|
| A | 原始基座 | 0.520 | +0.0026 | 0.27% | 0.6916 |
| B | 原始基座 | 0.539 | +0.0047 | 0.37% | 0.6893 |
| C | 原始基座 | 0.604 | +0.0473 | 1.81% | 0.6724 |
| **D** | **SFT 后** | **0.653** | **+0.1811** | **7.96%** | **0.6366** |

**C→D 只换起点**：margin **3.8×**、漂移 **4.4×**、loss 降幅 **2.7×**。

一个可直接读到的证据——两轮 DPO 的**初始** log-prob：

| 起点 | chosen − rejected gap |
|---|---|
| 原始基座（C） | **−7.84**（给 rejected 打分更高！） |
| **SFT 后（D）** | **+4.75** |

**gap 从负变正**，与探针测的 58.0% 相互印证。

### 9.3 POPE：项目最强的单项结果

| 指标 | baseline | SFT | **SFT+DPO** |
|---|---|---|---|
| **幻觉率** | 10.97% | 10.34% | **8.82%（−2.15）** |
| **precision** | 77.18 | 78.12 | **80.51（+3.33）** |
| recall | 89.73 | 89.27 | 88.13（−1.60） |
| **假阳性数** | 398 | 375 | **320（−78）** |
| 总正确数 / 5127 | 4575 | 4591 | **4629（+54）** |
| McNemar vs baseline | — | p=0.185 | **p=4.19×10⁻⁵** |

**DPO 自身贡献**（SFT → SFT+DPO）：幻觉率再降 1.52pt，
改对 61 / 改错 23，**p<0.0001**。

### 9.4 验证：这不只是「阈值平移」

recall 掉了 1.60，所以必须排除「模型一律少答 Yes」这个平凡解释：

```
答 Yes 比例 34.0% → 32.0%（真值 29.3%）—— 确实变谨慎了
在「不存在物体」半边修好 78 题 → 翻转率 2.15%
若纯粹是一律少答 Yes，同样翻转率应在「存在物体」半边损失约 32 题
实测只损失 24 题  →  翻转是有选择性的
```

分半边看：

| | baseline | SFT+DPO |
|---|---|---|
| 不存在物体（3627 题） | 89.0% | **91.2%（+2.15pt）** |
| 存在物体（1500 题） | 89.7% | 88.1%（−1.60pt） |
| **全集净增** | — | **+54 题正确** |

**是一次净正的重新校准 + 真实的判别力改善**，不是拿一个换另一个。

### 9.5 四项 MCQ：DPO 首次全部同向为正

| 指标 | SFT | SFT+DPO | DPO 贡献 |
|---|---|---|---|
| **MMBench** | 78.48 | **79.26** | **+0.78** |
| **MME 逐题** | 88.12 | **88.37** | **+0.25** |
| **AI2D** | 79.11 | **79.31** | **+0.20** |
| MMStar | 58.67 | 58.67 | ±0.00 |

**三正一平，没有负项**——四轮 DPO 里唯一一次。

同一个 DPO 阶段、只换起点的对比：

| DPO 的起点 | MME 逐题的增益 |
|---|---|
| 原始基座（C） | +0.04pt |
| **SFT 后（D）** | **+0.25pt（6 倍）** |

> **MMStar 的 ±0.00 值得一提**：1500 题里有 **161 题答案变了**，
> 分数却逐位相同。改对改错精确抵消，
> 和 C 组在 POPE 上的 12:12 是同一个特征，只是规模大 13 倍。
> **说明 DPO 的收益是分布相关的，不是全局的。**

### 9.6 HallusionBench：没能修回来

| 指标 | baseline | SFT | SFT+DPO |
|---|---|---|---|
| aAcc | 61.93 | 60.57 | 60.67（−1.26） |
| fAcc | 37.57 | 33.82 | 33.53（**−4.05**） |

DPO 只把 aAcc 拉回 0.10pt，fAcc 还继续掉了 0.29。
**SFT 造成的这处退化，DPO 没有修复。**

原因与 §8.6 一致：**不是同一种能力**，所以在 RLAIF-V 上做多少 DPO
都不会迁移到「抵抗视觉错觉」上。

### 9.7 完整因果链（闭环）

```
全参 SFT（20000 条 chosen 回答）
  → 分布内能力 ↑（loss 1.23 → 0.98）
  → 可分性 53.6% → 58.0%，信噪比 0.055 → 0.218（4×）
  → DPO 训练信号 3.8×（margin 0.047 → 0.181）
  → DPO 实际收益 6×（MME 逐题 +0.04pt → +0.25pt）
  → POPE 幻觉率 −2.15pt（p=4.2×10⁻⁵），四项 MCQ 全部非负
```

**前四轮 DPO 失败的诊断被完整验证：不是 DPO 不行，
是基座对那个分布建模不足。**

---

## 10. 复现步骤

```bash
# ---------- 环境（一次性，两条都必须） ----------
pip install --index-url https://pypi.org/simple/ "antlr4-python3-runtime==4.9.3"
pip install --only-binary=:all: "pyarrow==16.1.0"

export PYTHONPATH=$VITA_REPO LMUData=/root/LMUData WEIGHTS_ROOT=$VITA_WEIGHTS

# ---------- 1. baseline（六项，单卡约 1.5 小时） ----------
cd $VITA_REPO/VLMEvalKit
VITA_CKPT=$WEIGHTS_ROOT/VITA-1.5 python run.py \
    --data MME MMStar MMBench_DEV_EN_V11 AI2D_TEST POPE HallusionBench \
    --model vita_qwen2 --work-dir /path/eval_out/baseline
# 数据集下载：证书过期，需 curl -k 后用源码里的 MD5 校验（见 §3.3）
# 起评测前必须等 MD5 通过，否则会并发写坏文件（见 §11.6）

# ---------- 2. 数据 ----------
# HF 的 resolve/ 是重定向，文件大小要问 API，不能用 curl -I（见 §11.8）
curl -s "https://huggingface.co/api/datasets/openbmb/RLAIF-V-Dataset/tree/main" \
  | python3 -c "import json,sys;[print(f['path'],f['size']) for f in json.load(sys.stdin)]"

# DPO 格式
python tools/make_rlaif_v_data.py --parquet shard000.parquet shard001.parquet \
    --out-dir $WEIGHTS_ROOT/rlaif_v_dpo --limit 15000
# SFT 格式（只保留 chosen）
python tools/make_rlaif_v_sft_data.py --parquet shard00{0,1,3,4}.parquet \
    --out-dir $WEIGHTS_ROOT/rlaif_v_sft --limit 20000

# ---------- 3. 【关键】训练前先跑探针 ----------
VITA_RLAIF_DATA_DIR=$WEIGHTS_ROOT/rlaif_v_dpo VITA_CKPT=$WEIGHTS_ROOT/VITA-1.5 \
    python tools/probe_preference_separability.py --n 400
#   CI 跨过 50%       -> 换数据，别训
#   显著但 SNR < 0.2  -> 训得起来但不会变好
#   65%+              -> 可以训
# 至少跑 300-400 对，n=100 的 CI 宽到没有判读价值

# ---------- 4. 全参 SFT（8 卡 ZeRO-3，约 2 小时） ----------
VITA_SFT_DATA_DIR=$WEIGHTS_ROOT/rlaif_v_sft WORKERS=0 \
    bash script/train/sft_rlaif_v_8gpu.sh /path/sft_out

# ---------- 5. 重测可分性（应当明显上升） ----------
VITA_CKPT=/path/sft_out/sft-rlaif-v \
VITA_RLAIF_DATA_DIR=$WEIGHTS_ROOT/rlaif_v_dpo \
    python tools/probe_preference_separability.py --n 400

# ---------- 6. 从 SFT 起点跑 DPO（约 2.5 小时） ----------
MODEL_PATH=/path/sft_out/sft-rlaif-v \
VITA_RLAIF_DATA_DIR=$WEIGHTS_ROOT/rlaif_v_dpo \
WORKERS=0 GRAD_ACC=10 LR=2e-5 GPUS=2,3,4,5,6,7 \
    bash script/train/dpo_rlaif_v_8gpu.sh /path/dpo_from_sft
# 首步 loss 必须 ≈ 0.6931，否则参考模型接错了，停下来查

# ---------- 7. 合并 + 评测 + 对比 ----------
python tools/merge_and_eval.py --base /path/sft_out/sft-rlaif-v \
    --adapter /path/dpo_from_sft/dpo-rlaif-v-8gpu \
    --out $WEIGHTS_ROOT/VITA-1.5-sft-dpo
VITA_CKPT=$WEIGHTS_ROOT/VITA-1.5-sft-dpo python run.py --data ... \
    --work-dir /path/eval_out/sftdpo
python tools/compare_eval.py --before .../baseline/vita_qwen2 --after .../sftdpo/vita_qwen2
python tools/compare_pope.py --before .../baseline/vita_qwen2 --after .../sftdpo/vita_qwen2
python tools/summarize_all_runs.py     # 全部模型 × 全部指标
```

**时间成本（8×H100）**：

| 步骤 | 耗时 |
|---|---|
| baseline 六项 | 1.5 小时（单卡） |
| **探针 400 对** | **8 分钟** |
| 全参 SFT 20000 样本 | 1 小时 54 分 |
| DPO 15000 对 | 2 小时 26 分 |
| 评测六项 | 1.5 小时（3 卡并行 ~40 分） |

**那 8 分钟的探针能省掉后面 4 小时**——这是本项目最划算的一步。

---

## 11. 踩过的坑

### 11.1 antlr4 / omegaconf 冲突
见 §3.1。装 4.9.3（纯 Python 无需编译）。

### 11.2 LMUData 硬编码
`vlmeval/smp/file.py:71` 写死上游作者路径，症状伪装成 SSL 错误。

### 11.3 服务端证书过期
`curl -k` + 用源码里的 MD5 验真，比信任过期证书更严格。

### 11.4 pyarrow 源码编译失败
`--only-binary=:all:` 强制用 wheel。

### 11.5 deepspeed 端口冲突
同机第二个任务报 `Address already in use ... 29500`，
**报错栈 18 层深、最后一行才提到端口**。
`--include localhost:N` 只管分配哪张卡，不管通信端口——
每个任务给不同的 `--master_port`。

### 11.6 没下完就起评测 → 文件被写坏
POPE.tsv 1.1 GB，我在它下到 204 MB 时起了三个评测任务。
VLMEvalKit 校验 MD5 不匹配 → **三个任务各自重新下载同一文件** →
和原下载一起写同一路径 → **最终 1.1 GB 但 MD5 错误，看大小完全正常**。

报错是 `Failed to download`，**看起来像网络问题，实际是并发写冲突**。
正确做法：起任务前先等 MD5 通过。

### 11.7 `/dev/shm` 只有 512 MB
7 卡 × 4 dataloader worker 各自搬运解码后的 448×448 图块，撑爆共享内存，
报 `DataLoader worker killed by signal: Bus error`，容器内无权 remount。
`--dataloader_num_workers 0` 在进程内加载，不用共享内存；
瓶颈是 7B 前向，数据加载本来就不是。

### 11.8 HF 的 `resolve/` 是重定向
`curl -sI` 不加 `-L` 返回的是**重定向响应本身的 `Content-Length: 1034`**，
不是文件的 1 GB。拿这个数切分片，每段都是几百字节错误页，
`cat` 起来得到一个 1.1 KB 的「parquet」——**而且全程不报错**。

用 HF API 查真实大小，下完校验字节数。

### 11.9 `conv.get_prompt()` 缺 modality 参数
**这是自采样脚本所有 `AssertionError` 的真凶。**
`conversation.py:137` 会按 modality 在两个 system prompt 间选择，
消息里含 `<image>` 时不传就抛一个**无消息的断言**，
很容易误判成 tokenizer 问题。正确调用：`conv.get_prompt("image")`。

### 11.10 prompt 里 `<image>` 要重复 `p_num` 次
VITA 把图切成 p_num 个 tile，`vita_arch` 断言**一个 image feature 对一个
`<image>` token**。只写一个 `<image>` 配多 tile 像素会失败。

### 11.11 图像预处理曾是瓶颈（后来发现不是）
py-spy 抓到进程停在 `image_transforms.normalize`，我据此改成
每 prompt 预处理一次 + `num_return_sequences` 批量采样。
**但后来实测预处理只占 12 ms/图**，真正的耗时是 7B 生成。
改动本身没错（确实省了重复工作），但当时的归因是错的。

### 11.12 merge 把训练 checkpoint 也拷进去了
`merge_and_eval.py` 会拷 base 的所有子目录，好让产物自包含。
对发布版 checkpoint 没问题，但当 base 是我们自己的 SFT 输出时，
里面有 `checkpoint-600/`（优化器状态）——
**16 GB 的模型产出了 109 GB 的目录**。已加 `SKIP_PREFIXES` 过滤。

### 11.13 SFT checkpoint 的 audio encoder 路径
我们自己 SFT 产出的 checkpoint 把冻结编码器记成 **config 里的绝对路径**，
而非子目录。所以 `AUDIO_ENCODER` 从 `MODEL_PATH` 推导会失败——
**恰好在「SFT→DPO」这条链上**。已加 fallback。

---

## 12. 我判断错又更正的地方

保留这一节，因为**这些误判比结论更容易重复**。

### 12.1 把 n=100 的探针结果读成「等同随机」
第一次跑探针用 100 对，读到 53.0%，判断为「基座完全分不开」。
n=400 重测得到 55.2%、**p=0.036——显著高于随机，只是极弱**。

n=100 的置信区间宽到无法区分「没信号」和「弱信号」，
而这两者对下一步的指导完全不同。**探针至少跑 300–400 对。**

### 12.2 在训练进行到 20% 时判断「漂移不变」
C 轮跑到 44/238 步时，我看到漂移 0.293%、与 A 组的 0.269% 几乎一样，
据此写下「大 batch 让方向更准，但没让步子更大」。
跑到 145 步发现这是**早期采样的假象**——漂移全程涨 7.4 倍。

**低噪声梯度的收益是复利式的**，每步都朝同一方向推，
积累到后半程才明显。**看前 20% 会看反。**

### 12.3 把 MME 的 −80 分读成「远超噪声的真实退化」
SFT 后 MME 总分掉 80.34，我说这是「远超噪声」。拆开看：

| 指标 | baseline | SFT |
|---|---|---|
| 逐题正确率 | 88.50% | 88.12%（**−0.38pt**） |
| 双题全对率 | 78.26% | 77.42% |

MME 总分 = 两者相加，而 `acc_plus` 要求同图两问都对，
**所以一次翻错被扣两次**。真实退化是 0.38 个百分点，不是 3.4%。

### 12.4 说 C 轮「MME 掉 7.3 分是唯一超噪声的退化」
写汇总脚本时从 CSV 重算，发现 **C 轮的逐题正确率是 88.54%，
比 baseline 的 88.50% 还高**，双题全对率也升了。

总分却降了，因为 MME **按 14 个子项等权相加**，题目少的子项权重被放大：

```
numerical_calculation  147.50 → 140.00   −7.50   ← 只有 20 题
commonsense_reasoning  137.14 → 135.00   −2.14
landmark/scene/celebrity        共     +2.38
合计                                    −7.26
```

**翻错 20 题里的一题就是 −7.5 分。** 修正后 C 轮的结论更干净：
**既没变好也没变坏**，与 POPE 的 12:12 完全一致。

> **这是我对 MME 犯的两个计分错误（12.3 + 12.4）。教训：
> 报 MME 必须同时给逐题正确率**，它的尺度不是准确率。

### 12.5 用「更谨慎」解释 POPE 的改善
SFT 后 POPE 改善，我说是「模型更谨慎地宣称看见某物」。
查答案分布后**这个解释不成立**：

| Benchmark | 真值 Yes | baseline | SFT |
|---|---|---|---|
| POPE | 29.3% | 34.0% | 33.4%（↓） |
| HallusionBench | 42.2% | 66.6% | **71.1%（↑）** |

**SFT 没有让模型「一律更谨慎」，而是在不同任务上朝不同方向偏。**

### 12.6 错误归因图像预处理是瓶颈
见 §11.11。py-spy 两次抓到 `normalize`/`resize`，我据此判断
CPU 预处理是瓶颈；后来实测只占 12 ms/图，真正耗时是 7B 生成。

---

## 13. 产出清单

### 13.1 工具

| 文件 | 作用 |
|---|---|
| **`tools/probe_preference_separability.py`** | **训练前 8 分钟判断一批偏好数据值不值得跑**。报 accuracy、CI、显著性检验、信噪比 |
| `tools/make_rlaif_v_data.py` | RLAIF-V parquet → DPO 格式，图像 SHA-1 去重，过滤退化 pair |
| `tools/make_rlaif_v_sft_data.py` | 同源数据 → SFT 格式（只保留 chosen） |
| `tools/make_selfsample_data.py` | on-policy 自采样偏好对（8 卡并行，含三种判据的实测记录） |
| `tools/merge_selfsample_shards.py` | 合并分片输出（硬链接图像，不额外占盘） |
| `tools/compare_eval.py` | before/after 对比，**带 1.96σ 噪声带**；POPE 额外给 precision/recall 解读 |
| `tools/compare_pope.py` | 题级幻觉率 + McNemar 检验（口径与 VLMEvalKit 不同，见注释） |
| `tools/summarize_dpo_log.py` | DPO 日志压成趋势（分箱，处理 tqdm 回车符） |
| **`tools/summarize_all_runs.py`** | **全部模型 × 全部指标矩阵**，从 CSV 直读避免转录错误 |
| `tools/merge_and_eval.py` | LoRA adapter 合并进基座，产出 VLMEvalKit 可直接加载的 checkpoint |

### 13.2 训练脚本

| 文件 | 用途 |
|---|---|
| `script/train/dpo_rlaif_v.sh` | 单卡 LoRA DPO |
| `script/train/dpo_rlaif_v_8gpu.sh` | 多卡 LoRA DPO（含 `/dev/shm` 与 audio encoder 路径规避） |
| `script/train/sft_rlaif_v_8gpu.sh` | 8 卡全参 SFT（ZeRO-3） |

### 13.3 磁盘产物（本机，不在 git 里）

```
$VITA_WEIGHTS/rlaif_v/shard00{0,1,3,4}.parquet   原始分片，约 4 GB
$VITA_WEIGHTS/rlaif_v_dpo/                        3000 对（第 1-2 轮）
$VITA_WEIGHTS/rlaif_v_dpo_large/                  15000 对（第 3、6 轮）
$VITA_WEIGHTS/rlaif_v_sft/                        20000 条 SFT 样本
$VITA_WEIGHTS/selfsample/merged/                  2400 对 on-policy（第 4 轮）
$VITA_WEIGHTS/VITA-1.5-dpo{,-lr2e5,-large}/       前三轮合并后的 checkpoint
$VITA_WEIGHTS/VITA-1.5-sft-dpo/                   **最终模型**（16 GB）
$SFT_OUT/sft-rlaif-v/                             SFT checkpoint
$EVAL_OUT/{baseline,dpo,dpo_lr2e5,dpo_large*,sft*,sftdpo*}/   全部评测结果
```

### 13.4 相关文档

- `BENCHMARKS.md` —— 这台机器上的实测性能、显存、报错原文
- `HANDBOOK.md` —— 命令速查、地雷区、DPO/GRPO 操作章节
- `ARCHITECTURE.md` §14 —— RL 栈的代码走读
- `RESULTS.md` —— 早期版本（前三轮），已被本文档取代

### 13.5 尚未做的

| 项 | 状态 |
|---|---|
| **GRPO 在真实数据上** | 仅在 12 条合成数据上验证过代码路径。多模态扩展已写完（见下方"多模态 GRPO 扩展"），但尚未跑真实规模数据 |
| 语义判据（NLI / LLM 评分） | 第 4 轮的天花板在此，未实现 |
| SFT 混入通用数据 | 可能同时保住 OCR 与 HallusionBench，未验证 |
| SFT 早停 | `save_total_limit 1` 删掉了中间 checkpoint，验证需重训 |

#### 多模态 GRPO 扩展（image+text）

上游 GRPO 只支持纯文本 prompt。本 fork 把它扩到图像：

- **数据**（`vita/train/grpo_data.py`）：`GRPOPromptDataset` 新增图像路径——`_load_image_tiles` 用与 SFT 相同的 `dynamic_preprocess` + `image_processor` 切瓦片，`_build_image_prompt` 用 `preprocess_multimodal(patch_num=[n_tiles])` 把单个 `<image>` 展开成 n_tiles 份，`tokenizer_image_token` 每份发一个 `IMAGE_TOKEN_INDEX` 占位符。文本路径**不走** `preprocess_multimodal`（它会对每条 sentence 做 `"\n\n"→"\n"` 归一化、改变 tokenization、破坏首步 `grpo/kl==0` 恒等），直接 `conv.append_message` + `get_prompt("lang")`，与原版字节一致。
- **训练器**（`vita/train/grpo_trainer.py`）：加 `_fuse`——仿 `VITADPOTrainer._fuse` 但**不传** `image_group_size`。GRPO 每个 prompt 自带图，G 倍复制在 fusion 之后的 embedding 上做纯 repeat，所以 vision tower 每 batch 只跑一次（按 distinct image），无需像 DPO 那样对 chosen/rejected 共享图去重。`compute_loss` 用 `any(has_image)` 分流：图像走 `_fuse` 得到 `inputs_embeds` 当 `prompt_embeds`，文本走原 `embed_tokens(input_ids)`。后续 `_rollout`/`_sequence_logps`/`grpo_loss` 全不变。
- **smoke**（`script/train/grpo_mm_smoke_test.sh` + `tools/make_grpo_mm_smoke_data.py`）：8 条带图记录，`reward_meta` 的 keywords 取图里真实可见的词，让 `keyword_reward` 能给分组排序梯度。

状态：**代码完成、`py_compile` 通过、设计逐条对照源码核实**（`prepare_inputs_labels_for_multimodal` 容忍 `labels=None`；图像占位符断言成立；fusion 在 expand 之前跑）。运行时 smoke 验证待环境重建后进行——首步 `grpo/kl≈0` 是通过条件。

---

## 附：如果只记住三件事

1. **训练前量数据可分性，再决定投不投算力。**
   本项目在「调超参」和「加规模」上花了约 4 小时 GPU，
   而探针只要 8 分钟就能给出同样的判断。

2. **在弱信号下，「训得起来」和「训得更好」是两件事。**
   C 轮把 margin 提到 18 倍、漂移 6.7 倍，POPE 上却是 12 改对 / 12 改错——
   位移放大，期望收益精确为零。

3. **可分性是模型的属性，不只是数据的属性。**
   同一批数据，SFT 前 52.2%、SFT 后 58.0%。
   「数据不可分」有时候意味着「该先改进模型」，而不是「该换数据」。
