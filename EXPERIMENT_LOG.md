# VITA-1.5 上的 DPO 完整实验记录

> **一句话**：评测链路从零打通、四项 baseline 对齐论文；在 RLAIF-V 上做了
> 三轮 DPO，全部没有产生超出噪声的提升。**根因经六项 benchmark、三组对照
> 实验和一个自建探针定位到唯一变量：这批偏好数据对 7B 基座几乎不可分
> （判别力 53.6%，信噪比 0.055–0.11）。**
>
> 采集于 2026-08-09，8×H100。所有数字均为实测，可用本文 §9 复现。
>
> 这份文档是**完整过程记录**，包含走过的弯路和我判断错又改正的地方。
> 只想看数字看 [§2](#2-最终结果一张表)；只想看结论看 [§7](#7-结论)；
> 想复现看 [§9](#9-复现步骤)。

---

## 目录

- [1. 实验设计](#1-实验设计)
- [2. 最终结果：一张表](#2-最终结果一张表)
- [3. 起点：把评测链路修通](#3-起点把评测链路修通)
- [4. 数据：RLAIF-V](#4-数据rlaif-v)
- [5. 三轮训练](#5-三轮训练)
- [6. 逐个排除假设](#6-逐个排除假设)
- [7. 结论](#7-结论)
- [8. 走过的弯路](#8-走过的弯路)
- [9. 复现步骤](#9-复现步骤)
- [10. 产出清单](#10-产出清单)

---

## 1. 实验设计

**目标**：给 VITA-1.5（7B 多模态模型）加一个 RL 阶段，并测出它有没有用。

**为什么这件事需要设计**：此前本仓库**一个 benchmark 数字都没有**——
`import vlmeval` 就失败。没有 baseline 的话，训练后哪怕拿到一个数，
也无法判断它是提升、退化还是噪声。所以整个实验分两步：

1. **先建立可信的标尺**：跑通评测，且数字必须落在论文附近。
   一个数字仅仅「存在」证明不了什么；落在论文值附近才同时证明了
   prompt 模板、图像预处理、答案抽取三件事都对。
2. **再做干预并对比**：同一套标尺量 before/after。

**关键的方法论选择**：每个 delta 都必须和**噪声带**一起报。
MMStar 上 1.96σ 是 ±2.5 分——不知道这个数的话，`+0.3` 会被当成提升。

---

## 2. 最终结果：一张表

### 2.1 六项 benchmark × 四个模型

| Benchmark | baseline | A (3k/bs16/5e-6) | B (3k/bs16/2e-5) | C (15k/bs63/2e-5) | 噪声带 1.96σ |
|---|---|---|---|---|---|
| MME (total) | **2353.51** | 2352.38 (−1.12) | 2352.11 (−1.39) | 2346.25 (**−7.26**) | — |
| MMStar | **59.80** | 59.80 (±0.00) | 59.93 (+0.13) | 60.07 (+0.27) | ±2.48 |
| MMBench_DEV_EN_V11 | **77.79** | 77.71 (−0.08) | 77.79 (±0.00) | 78.02 (+0.23) | ±2.27 |
| AI2D_TEST | **79.24** | 79.27 (+0.03) | 79.27 (+0.03) | 79.44 (+0.19) | ±1.43 |
| HallusionBench aAcc | **61.93** | 61.93 (±0.00) | 61.83 (−0.11) | 61.83 (−0.11) | — |
| HallusionBench fAcc | **37.57** | 37.57 (±0.00) | 37.28 (−0.29) | 37.28 (−0.29) | — |

### 2.2 POPE（幻觉专项，5127 题）

| 模型 | 幻觉率 | precision | recall | 假阳性 | 改变题数 | 改对 | 改错 | McNemar p |
|---|---|---|---|---|---|---|---|---|
| baseline | **10.97%** | 77.18 | 89.73 | 398 | — | — | — | — |
| A (5e-6) | 11.00% | 77.13 | 89.73 | 399 | 5 | 2 | 3 | 1.000 |
| B (2e-5) | 11.11% | 76.96 | 89.73 | 403 | 13 | 4 | 9 | 0.267 |
| **C (15k/bs63)** | **10.97%** | **77.18** | **89.73** | **398** | **24** | **12** | **12** | **1.000** |

> **C 组那一行是整个实验最干净的证据。** 改变的题数是 A 组的 5 倍，
> 但 12 改对 / 12 改错、Yes→No 与 No→Yes 各 12 题——**完全对称**，
> 所有聚合指标逐位相同。这是「噪声驱动更新」的标准 signature：
> 位移变大，净方向为零。

### 2.3 baseline 对齐论文（这是整个实验的地基）

| Benchmark | 实测 | 论文 | 差 |
|---|---|---|---|
| MME | 2353.51 | 2362.0 | −0.36% |
| MMStar | 59.80 | 60.2 | −0.4 |
| MMBench | 77.79 | 76.6 | +1.2 |
| AI2D | 79.24 | 79.3 | −0.06 |

---

## 3. 起点：把评测链路修通

`import vlmeval` 失败，背后叠了四个独立问题。按踩到的顺序：

### 3.1 antlr4 / omegaconf 版本冲突

```
Exception: Could not deserialize ATN with version 3 (expected 4).
```

`omegaconf 2.3.1` 的语法文件由 antlr4 4.9 系列生成，企业镜像最低 4.11。

**解**：从上游 PyPI 装 4.9.3。它是纯 Python 包——这点关键，
因为本机 gcc 4.8.5 编译不了任何东西。

```bash
pip install --index-url https://pypi.org/simple/ "antlr4-python3-runtime==4.9.3"
```

降级后复查 `torch 2.3.1+cu121`、`transformers 4.41.1` 均未受影响。

### 3.2 数据集根目录被硬编码成上游作者的机器

`vlmeval/smp/file.py:71` 写死 `/mnt/cfs/lhj`。本机没有，于是它
**无视已下载的文件、每次都重新下载**。设 `LMUData` 环境变量即可。

**这个坑最坑的地方是它伪装成别的问题**：你看到的是 SSL 报错（见下），
真正的原因隔了两层。

### 3.3 opencompass 服务器证书过期

```
NSS error -8181 (SEC_ERROR_EXPIRED_CERTIFICATE)
```

`openssl s_client` 查得 `*.openxlab.space` 叶证书 `NotAfter: Apr 16 2026`，
而当天是 8 月 9 日——**服务端过期，不是本机 CA 库问题**（换 certifi 一样失败）。

**解**：`curl -k` 下载后**用 VLMEvalKit 源码里存的 MD5 校验**。
MD5 来自 git 仓库，是独立于传输通道的可信来源，篡改会被检出——
这比信任一张过期证书更严格。四个数据集全部校验通过。

### 3.4 pyarrow 必须走预编译 wheel

`pip install pyarrow` 尝试源码编译并死在 gcc 4.8.5。
`pip install --only-binary=:all: "pyarrow==16.1.0"` 秒过。

---

## 4. 数据：RLAIF-V

**RLAIF-V-Dataset**（openbmb），5 个分片约 34000 对，实际用 15000 对。
转换脚本 `tools/make_rlaif_v_data.py`。

### 4.1 转换脚本必须处理的三件事

| 事项 | 不做会怎样 |
|---|---|
| **图像按内容寻址**（SHA-1 做文件名） | parquet 里图是内嵌字节且跨行重复，逐行写会存一堆副本。15000 对实际只有 8821 张不同的图 |
| **补 `<image>` token** | `LazySupervisedDataset` 靠这个字面量决定往哪插视觉特征，**缺了会静默退化成纯文本训练**，loss 曲线照常好看 |
| **丢掉两侧相同的 pair** | DPO logit 恒为 0，loss 永远 `-log(0.5)`，白跑前向。实测 15297 条里有 21 条 |

### 4.2 数据体检（训练前就该看的数字）

| 指标 | 3000 对 | 15000 对 | 判读 |
|---|---|---|---|
| chosen 平均长度 | 299 字符 | 307 | |
| rejected 平均长度 | 298 字符 | 307 | 几乎相同 |
| chosen 更长的比例 | 46.5% | 47.1% | **无长度偏置**（50% 为完全无偏） |
| 两回答文本相似度 | 0.29 | 0.294 | **是两个不同的答案**，不是改词 |

**这两个「好」指标是双刃剑**：没有长度捷径、没有格式捷径，
两个回答都流畅切题、只在某个细节是否幻觉上不同。
**RLAIF-V 作为高质量偏好数据的优点，正是它对 7B 基座很陡的原因。**

### 4.3 链路正确性检查

**首步 loss = 0.6931，精确命中 `-log(0.5)`。**

这是数学恒等式不是经验值：未训练的 LoRA 让 policy 等于 reference，
DPO logit 为零。**这是判断参考模型接线是否正确的最强信号，而且免费。**
合成数据和真实数据上都精确命中。

---

## 5. 三轮训练

### 5.1 配置与训练指标

| 轮次 | 数据 | 有效 batch | LR | 步数 | 耗时 | accuracy | margin | 策略漂移 | loss(末5) | grad_norm |
|---|---|---|---|---|---|---|---|---|---|---|
| **A** | 3000 | 16 | 5e-6 | 187 | 50 min | 0.520 | +0.0026 | 0.269% | 0.6916 | 0.39 |
| **B** | 3000 | 16 | **2e-5** | 187 | 47 min | 0.539 | +0.0047 | 0.372% | 0.6893 | 0.42 |
| **C** | **15000** | **63** | 2e-5 | 238 | 143 min | **0.604** | **+0.0473** | **1.81%** | **0.6724** | 0.30 |

> 「策略漂移」= `|policy_logp − ref_logp| / |seq_logp|`，
> 即模型相对基座偏离了多少。**benchmark 响应的是这个量，不是 margin。**

### 5.2 C 组：唯一真正学起来的一轮

把有效 batch 从 16 提到 63（7 卡 × 累积 9）之后：

- **loss 首次真正跌破 0.69**：A/B 组末段 0.6916/0.6893 距离 `-log(0.5)=0.6931`
  只有 0.002–0.004，基本还贴在零点；C 组到 0.6724。
- **margin 是 A 组的 18 倍**，accuracy 0.604。
- **策略漂移是 A 组的 6.7 倍**。
- **grad_norm 从 0.39 降到 0.30**——平均 63 个样本后梯度噪声确实降了，
  符合 √G 的预期。

按五等分看 C 组的漂移演化（跑完后的完整数据，n=2142）：

| 阶段 | 策略漂移 | margin | accuracy |
|---|---|---|---|
| Q1 | 0.266% | +0.0039 | 0.521 |
| Q2 | 0.458% | +0.0084 | 0.563 |
| Q3 | 1.283% | +0.0386 | 0.607 |
| Q4 | 1.788% | +0.0501 | 0.624 |
| Q5 | **1.959%** | +0.0467 | 0.579 |

**漂移全程涨 7.4 倍**，而 A/B 组是全程卡在 0.27% 不动。
注意 Q3→Q5 漂移仍在涨但 margin/accuracy 已经见顶回落——
**这正是「继续沿噪声方向前进」的样子：位移还在累积，收益已经没有了。**


### 5.3 但评测没有变好

C 组六项：三升三降（见 §2.1）。唯一超出噪声带的是 MME 的 −7.26。

**这就是「漂移变大」的双面性**：

- 0.27% 漂移（A 组）→ 模型几乎没变 → benchmark 不动
- 1.81% 漂移（C 组）→ 模型**真的变了** → benchmark **略微变差**

信噪比 0.055 意味着模型学到的「偏好方向」里噪声占 95%。
**沿着这个方向走得越远，就越是在随机扰动一个原本校准良好的模型。**

MME 降 7.26 和 POPE 的 12:12 对称，是同一件事的两个侧面。

---

## 6. 逐个排除假设

这是本实验的核心方法：**每个候选原因用一个专门的对照实验排除。**

| # | 候选原因 | 排除方式 | 结果 |
|---|---|---|---|
| 1 | 梯度消失 | 看 grad_norm | 全程 0.20–0.45，健康 → 排除 |
| 2 | 参考模型接错 | 首步 loss | 精确 = 0.6931 → 排除 |
| 3 | 数据格式错 | 校验 `<image>` token、图像文件、pair 结构 | 全部正确 → 排除 |
| 4 | **学习率不够** | **B 组：LR × 4** | 训练指标明显更好（acc 0.539 vs 0.520），**评测完全一样** → 排除 |
| 5 | **benchmark 不对口** | **POPE + HallusionBench** | 5127 题只动 5 题、p=1.000；HallusionBench 逐位相同 → 排除 |
| 6 | **规模不够** | **C 组：数据 ×5、batch ×4** | **模型真的动了（漂移 6.7×），但没变好** → 排除 |
| 7 | **数据可分性不足** | **探针** | **53.6%、信噪比 0.055–0.11 ← 唯一剩下的** |

### 6.1 假设 5 值得展开：为什么要补测幻觉专项

前四个 benchmark 有个**方法论弱点**：MME 是 Yes/No，其余三个是选择题，
而 **RLAIF-V 是为降低长文本生成的幻觉设计的**。就算 DPO 真减少了幻觉，
这四把尺子也可能量不出来。

所以补跑 POPE（5127 题，71% 问的是图中不存在的物体——最对口的尺子）
和 HallusionBench。结果见 §2.2：**换了对口的尺子，结果一样**，
而且漂移方向是略微变差，不是「有提升但没测出来」。

### 6.2 可分性探针

`tools/probe_preference_separability.py` 用冻结的基座直接给
chosen/rejected 打分，看它能不能分对。

```
3000 对那批（n=400）:  55.2%  CI [50.4%, 60.1%]  p=0.036  信噪比 0.11
15000 对那批（n=500）: 52.2%  CI [47.8%, 56.6%]  p=0.325  信噪比 0.055
两批合并（n=900）:     53.6%  CI [50.3%, 56.8%]  p=0.033
```

**判别力显著高于随机（p=0.033），但只高 3.6 个百分点。**
log-prob 差均值 +3.89，标准差 35.87——信噪比 0.11。

DPO 的梯度正比于「模型当前错得有多系统」。信噪比 0.11 意味着
**每个 batch 的梯度方向几乎被噪声主导**。

---

## 7. 结论

### 7.0 第四轮：on-policy 自采样（2026-08-10 追加）

前三轮把原因收敛到「数据可分性」。查 RLAIF-V 的 `origin_split` 元数据
发现了比「可分性低」更根本的一层：

| 字段 | 值 |
|---|---|
| 生成 chosen/rejected 的模型 | **OmniLMM-12B** |
| 判定优劣的模型 | **OmniLMM-12B** |

**两个回答都不是 VITA 写的。** DPO 的前提是「模型自己会犯这个错，
我们纠正它」；RLAIF-V 把任务变成了「给另一个 12B 模型的两个回答排序」
——分布外任务。这解释了为什么加学习率和加规模都无效：
那个方向本来就与 VITA 的错误模式无关。

**修复**：`tools/make_selfsample_data.py` 让 VITA 自己对同一张图采样 6 个
回答，用 RLAIF-V 的 `chosen` 仅作**排序参考**（不进训练数据），
这样 pair 保持 on-policy。8 卡并行 15 分钟产出 2400 对。

**探针结果**：

| 数据 | accuracy | 95% CI | logp gap sd | 信噪比 |
|---|---|---|---|---|
| RLAIF-V（off-policy） | 53.6% | [50.3%, 56.8%] | 35.87 | 0.108 |
| 自采样（on-policy） | 54.8% | [49.9%, 59.6%] | **26.72** | **0.141** |

**accuracy 没有显著改善（54.8% vs 53.6%，CI 大幅重叠）。**
但看第三、四列：**log-prob 差的标准差降了 26%，信噪比升了 30%**。

> **这两个数说的是不同的事，值得分清**：
> accuracy 衡量「方向对不对」，由**判优劣的判据**决定；
> sd 衡量「打分稳不稳」，由**候选回答来自谁**决定。
> on-policy 确实把方差压下来了（这是它该起的作用），
> 但**判据的噪声决定了 accuracy 上不去**。

实测词袋判据约 **1/10 的对是判反的**。典型案例：
「leather crafter 还是 paper crafter」，错的答案因为共享
`hole`/`punch`/`scissors` 三个无关词拿到 0.50，
对的答案只有 `leather` 一个决定性词拿 0.31——**判反了**。
给「分歧词」加权无效，因为那些无关词也在分歧集里。

**追加验证：筛「高置信」对也没用。**

自然的补救想法是只保留判据最有把握的对（参考相似度差 >0.35，
共 634 对）。实测：

| 数据 | accuracy | 95% CI | 信噪比 |
|---|---|---|---|
| RLAIF-V 全量 | 53.6% | [50.3%, 56.8%] | 0.108 |
| 自采样 全量 | 54.8% | [49.9%, 59.6%] | 0.141 |
| 自采样 **高置信子集** | **53.2%** | [48.4%, 58.1%] | 0.117 |

**高置信子集反而更低。** 这一条否掉了「筛数据」这条廉价路线，
同时说明了判据坏在哪里：

> **参考相似度差距衡量的是「词面重叠差多少」，
> 不是「判对的概率有多高」。** 差距大往往只意味着
> 一个回答啰嗦、另一个简短，与哪个正确无关。
> 一个真正有信息量的置信度应当与正确率正相关——这个不是。

**结论：换成 on-policy 解决了方差，但没解决判据。**
信噪比 0.141 仍低于 0.2 的经验门槛，
**按本项目自己的判读标准，这批数据不该直接投入训练**——
那正是前三轮已经证明过的错误（在弱信号上加算力）。

而且「筛高置信」这条捷径也已被实测排除，
所以下一步只剩两条实质路线：

1. **语义判据**：用 NLI 模型判蕴含，或用更强 VLM 打分。
   直接修 1/10 判反的问题。
2. **先做全参 SFT**：用 chosen 做 SFT **不需要判优劣**，
   只需要好答案。绕开判据难题，且能提升基座在该分布上的能力，
   之后 DPO 的可分性会自然改善。

### 7.1 最核心的一条

> **在弱信号数据上，「训得起来」和「训得更好」是两件事。**

C 组证明了加大 batch 能让 DPO 在这批数据上真正学起来
（loss 跌破 0.69、margin 18×、漂移 6.7×），
**也同时证明了信噪比不足时，学起来不等于变好**。

POPE 的 12 改对 / 12 改错是这句话最精确的表达：
**位移放大了 5 倍，期望收益精确为零。**

### 7.2 这对任何 RLHF/DPO 项目的意义

**先量数据可分性，再决定投不投算力。**

反过来做的代价很具体：本项目在「调超参」和「加规模」上花了
约 4 小时 GPU 时间，而探针只要 8 分钟就能给出同样的判断。

判读标准（探针至少跑 300–400 对，n=100 的置信区间宽到无法区分
「随机」和「弱信号」——这是我自己踩过的）：

| 探针结果 | 判读 | 建议 |
|---|---|---|
| ~50%，CI 跨过 50% | 不可分 | 换数据，别训 |
| 显著但信噪比 < 0.2 | **真实但太弱** | 训得起来，但不会变好（**本项目**） |
| 65%+ | 信号充足 | 可以训；此时 margin 还平坦才该查 trainer |

### 7.3 下一步（按性价比排序）

1. **自建偏好对**（推荐）。用基座自己采样多个回答、用规则或更强模型
   判优劣。**pair 天然落在基座的能力边界上，可分性有保证**——
   这正是 RLAIF-V 缺的东西。GRPO 的 rollout 代码可直接复用。
2. **换更可分的公开数据**。探针跑一遍就知道值不值得。
3. **先 SFT 再 DPO**。基座在 RLAIF-V 分布上本来就弱，
   先用 chosen 回答做 SFT 提升分布内能力，再做偏好优化。

**不推荐**：继续加数据或加 batch。C 组已证明这条路在这批数据上
「有效但无益」。

---

## 8. 走过的弯路

保留这一节是因为**这些坑比结论更容易重复踩到**。

### 8.1 我判断错又改正的两处

**① n=100 的探针结果读成了「等同随机」。**
第一次跑探针用 100 对，读到 53.0%，我判断为「基座完全分不开」。
n=400 重测得到 55.2%、p=0.036——**显著高于随机，只是极弱**。
n=100 的置信区间宽到无法区分「没信号」和「弱信号」，
而这两者对下一步怎么做的指导完全不同。
**探针至少跑 300–400 对。**

**② 在训练进行到 20% 时判断「漂移不变」。**
C 组跑到 44/238 步时，我看到漂移 0.293%、与 A 组的 0.269% 几乎一样，
据此写下「大 batch 让方向更准，但没让步子更大」。
跑到 145 步发现这是**早期采样的假象**——漂移在加速，全程涨 7.4 倍。
**低噪声梯度的收益是复利式的**，每步都朝同一方向推，
积累到后半程才明显。看前 20% 会看反。

### 8.2 环境与工程坑

| 坑 | 症状 | 解 |
|---|---|---|
| antlr4 版本冲突 | `Could not deserialize ATN` | 装 4.9.3（纯 Python，无需编译） |
| LMUData 硬编码 | 无视已下载文件、报 SSL 错 | `export LMUData=...` |
| 服务端证书过期 | `SEC_ERROR_EXPIRED_CERTIFICATE` | `curl -k` + 用源码里的 MD5 验真 |
| pyarrow 源码编译 | 死在 gcc 4.8.5 | `--only-binary=:all:` |
| **deepspeed 端口冲突** | 同机第二个任务 `Address already in use`，栈有 18 层深 | 每个任务给不同 `--master_port` |
| **`/dev/shm` 只有 512M** | 7 卡 × 4 worker → `Bus error`，容器内无权 remount | `--dataloader_num_workers 0` |
| **没下完就起评测** | 三个任务同时重下同一文件，写坏；报错却说 `Failed to download` | 起任务前先等 MD5 校验通过 |
| **HF `resolve/` 是重定向** | `curl -I` 不加 `-L` 返回 1034 字节，切分片得到 1.1KB 的假 parquet，**全程不报错** | 用 HF API 查真实大小，下完校验字节数 |

### 8.3 一个值得记住的细节：为什么「逐位相同」是可信的

`vita_qwen2.py` 用 `do_sample=False` 贪心解码，**推理是确定性的**。
所以重跑同一个模型会得到完全相同的分数——「44/50 子项逐位相同」
不是采样噪声掩盖了变化，而是**模型对绝大多数题的答案确实没变**。

这也意味着 §2.1 那些噪声带的含义要说准确：它们衡量的是
**「换一批题目会有多大波动」**，不是「同一批题重跑的波动」（后者是 0）。

一个佐证：A 组改变的 5 道 POPE 题里，**3 道也被 B 组改变**。
两个独立训练的 adapter 动的是**同一批边界题**，
符合「决策边界上的题被轻微推动」这个图像，而非随机翻转。

---

## 9. 复现步骤

```bash
# ---------- 环境（一次性，两条都必须） ----------
pip install --index-url https://pypi.org/simple/ "antlr4-python3-runtime==4.9.3"
pip install --only-binary=:all: "pyarrow==16.1.0"

export PYTHONPATH=$VITA_REPO LMUData=/root/LMUData WEIGHTS_ROOT=$VITA_WEIGHTS

# ---------- 1. baseline（六项，单卡约 1.5 小时） ----------
cd $VITA_REPO/VLMEvalKit
VITA_CKPT=$WEIGHTS_ROOT/VITA-1.5 python run.py \
    --data MME MMStar MMBench_DEV_EN_V11 AI2D_TEST POPE HallusionBench \
    --model vita_qwen2 --work-dir /path/eval_out/baseline

# ---------- 2. 数据 ----------
# HF 的 resolve/ 是重定向，文件大小要问 API，不能用 curl -I
curl -s "https://huggingface.co/api/datasets/openbmb/RLAIF-V-Dataset/tree/main" \
  | python3 -c "import json,sys;[print(f['path'],f['size']) for f in json.load(sys.stdin)]"
# 分段并行下载后校验字节数，再转换
python tools/make_rlaif_v_data.py --parquet shard000.parquet shard001.parquet \
    --out-dir $WEIGHTS_ROOT/rlaif_v_dpo --limit 15000

# ---------- 3. 【关键】训练前先跑探针 ----------
VITA_RLAIF_DATA_DIR=$WEIGHTS_ROOT/rlaif_v_dpo VITA_CKPT=$WEIGHTS_ROOT/VITA-1.5 \
    python tools/probe_preference_separability.py --n 400
#   CI 跨过 50%       -> 换数据，别训
#   显著但 SNR < 0.2  -> 训得起来但不会变好（本项目就是这种）
#   65%+              -> 可以训

# ---------- 4. 训练（8 卡；/dev/shm 小的机器必须 WORKERS=0） ----------
VITA_RLAIF_DATA_DIR=$WEIGHTS_ROOT/rlaif_v_dpo WORKERS=0 GRAD_ACC=9 LR=2e-5 \
    bash script/train/dpo_rlaif_v_8gpu.sh /path/dpo_out
# 首步 loss 必须 ≈ 0.6931，否则参考模型接错了，停下来查

# ---------- 5. 合并 + 评测 + 对比 ----------
python tools/merge_and_eval.py --base $WEIGHTS_ROOT/VITA-1.5 \
    --adapter /path/dpo_out/dpo-rlaif-v-8gpu --out $WEIGHTS_ROOT/VITA-1.5-dpo
VITA_CKPT=$WEIGHTS_ROOT/VITA-1.5-dpo python run.py --data ... --work-dir /path/eval_out/dpo
python tools/compare_eval.py --before /path/eval_out/baseline/vita_qwen2 \
                             --after  /path/eval_out/dpo/vita_qwen2
python tools/compare_pope.py --before /path/eval_out/baseline/vita_qwen2 \
                             --after  /path/eval_out/dpo/vita_qwen2
```

**时间成本参考（8×H100）**：

| 步骤 | 耗时 |
|---|---|
| baseline 六项 | 1.5 小时（单卡） |
| **探针 400 对** | **8 分钟** |
| 15000 对训练（7 卡，batch 63） | 2.4 小时 |
| DPO 后评测六项 | 1.5 小时 |

**那 8 分钟的探针能省掉后面 4 小时**——这是本项目最划算的一步。

---

## 10. 产出清单

### 10.1 工具

| 文件 | 作用 |
|---|---|
| `tools/probe_preference_separability.py` | **训练前 8 分钟判断一批偏好数据值不值得跑**。报 CI、显著性检验、信噪比 |
| `tools/make_rlaif_v_data.py` | RLAIF-V parquet → DPO 格式，图像 SHA-1 去重，过滤退化 pair |
| `tools/compare_eval.py` | before/after 对比，**带 1.96σ 噪声带** |
| `tools/compare_pope.py` | 幻觉率（题级口径）+ McNemar 检验 |
| `tools/summarize_dpo_log.py` | 把 DPO 日志压成趋势（分箱，处理 tqdm 的回车符） |
| `tools/merge_and_eval.py` | LoRA adapter 合并进基座，产出 VLMEvalKit 能直接加载的 checkpoint |
| `script/train/dpo_rlaif_v.sh` | 单卡 DPO |
| `script/train/dpo_rlaif_v_8gpu.sh` | 多卡 DPO，含 `/dev/shm` 规避 |

### 10.2 数据与模型（留在磁盘上）

```
$VITA_WEIGHTS/rlaif_v/shard00{0,1,3,4}.parquet   原始分片，约 4 GB
$VITA_WEIGHTS/rlaif_v_dpo/                        3000 对（旧）
$VITA_WEIGHTS/rlaif_v_dpo_large/                  15000 对 + 8821 张图，1.3 GB
$VITA_WEIGHTS/VITA-1.5-dpo{,-lr2e5,-large}/       三个合并后的 checkpoint，各 19 GB
$EVAL_OUT/{baseline,dpo,dpo_lr2e5,dpo_large*}/    全部评测结果
$DPO_OUT{,_lr2e5,_large}/                         三轮训练日志与 adapter
```

> 这些是**本机产物，不在 git 里**（`.gitignore` 已排除权重与数据）。
> 换机器后按 §9 重新生成。

### 10.3 相关文档

- `BENCHMARKS.md` —— 这台机器上的实测性能、显存、报错原文
- `HANDBOOK.md` —— 命令速查、地雷区、DPO/GRPO 章节
- `ARCHITECTURE.md` §14 —— RL 栈的代码走读
- `RESULTS.md` —— 本文档的前身（较早的版本，保留作对照）
